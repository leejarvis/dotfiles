#!/usr/bin/env ruby

require "date"

ALLOWANCE = 10_000 # per year
DATE      = Date.new(2016, 5, 20)

daily_allowance = ALLOWANCE / 365
days            = (Date.today - DATE).to_i

puts "Car was received on #{DATE}. #{days} days ago."
puts "Average mileage should be at: #{daily_allowance * days}"
