#!/usr/bin/env ruby

merge_tags = {
  "%recipient.firstname%" => "Bob",
  "%sender.firstname%" => "Lee"
}

content = DATA.read

puts content.gsub(Regexp.union(merge_tags.keys), merge_tags)

__END__
Hi %recipient.firstname%,

Some email content.

Regards,
%sender.firstname%
